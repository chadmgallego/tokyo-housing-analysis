# Processed Data
- This folder contains the cleaned and transformed dataset, scraped from *SUUMO.jp*, that was used to train and evaluate the regression model.
- The pre-transformed dataset stored in SQLite is not included, as it contains the same data with fewer engineered features.

